# General dependencies
Flask==2.1.0
requests==2.28.1

# Cryptography for encrypting and decrypting data
cryptography==38.0.1

# For working with OpenSSL and system commands
pyOpenSSL==23.0.0

# For handling JSON data
jsonschema==4.17.0

# For creating reports
Jinja2==3.1.2

# For downloading and extracting vulnerability data
requests==2.28.1

# For handling archives and compressed files
zipfile36==0.1.0

# Python Date utilities
python-dateutil==2.8.2

# Required for system activity data collection using sar command
psutil==5.9.0

# For PDF conversion (if needed for the custom report formats)
pdfkit==1.0.0

# lxml for parsing HTML/XML when working with data scraping or HTML report generation
lxml==4.9.1
